// TODO: SOCKETS TESTING

const mocha = require('mocha');

describe('Socket testing', () => {
	it('should start a connection', (done) => {
		console.log('Socket tests not yet integrated');
		done();
	});
});