import { Component } from '@angular/core';
import { Router } from '@angular/router';

@Component({
    selector: 'subporter',
    template: `
		<router-outlet></router-outlet>
	`
})

export class App { };